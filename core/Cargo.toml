[package]
name    = "ztap-core"
version.workspace = true
edition.workspace = true
license.workspace = true
build   = "build.rs"

[dependencies]
serde   = { version = "1", features = ["derive"] }
bincode = "1.3"

[build-dependencies]
serde   = { version = "1", features = ["derive"] }
bincode = "1.3"
